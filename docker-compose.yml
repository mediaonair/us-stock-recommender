services:
  agent:
    build: .
    container_name: us-stock-recommender-agent
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
    restart: unless-stopped
